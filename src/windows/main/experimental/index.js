const {
    enableOnlineIndicator
} = require('./enableOnlineIndicator');
const {
    enableQuickReplies
} = require('./enableQuickReplies');
const {
    zipChats
} = require('./zipChats');
const {
    getActiveChat
} = require('./getActiveChat');

module.exports = {
    enableOnlineIndicator,
    enableQuickReplies,
    getActiveChat,
    zipChats
}